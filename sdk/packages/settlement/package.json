{
  "name": "@acl/settlement",
  "version": "0.1.0",
  "description": "ERC-8183 lifecycle helpers for ACL: createJob, setProvider, setBudget, fund (with USDC approve), submit, and ACLEvaluator.settle.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -b",
    "test": "bun test src"
  },
  "dependencies": {
    "@acl/core": "workspace:*",
    "viem": "^2.21.0"
  }
}
